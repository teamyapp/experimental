apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: production
  name: identity
  labels:
    app: identity
  annotations:
    # force policy will ensure that deployment is updated
    # even when tag is unchanged (latest remains)
    keel.sh/policy: force
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: identity
  template:
    metadata:
      labels:
        app: identity
    spec:
      containers:
        - name: identity
          image: harbor.internal.teamyapp.com/internal/identity:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
      imagePullSecrets:
        - name: harbor-cred
---
apiVersion: v1
kind: Service
metadata:
  namespace: production
  name: identity
spec:
  selector:
    app: identity
  ports:
    - name: http
      port: 80
      targetPort: 8080
---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  namespace: production
  name: identity
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`identity.teamyapp.com`)
      services:
        - kind: Service
          name: identity
          namespace: production
          port: 80
          passHostHeader: true
  tls:
    secretName: teamy-cert

